DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=iot_desalinasi
DB_PORT=3306

# Server Configuration
PORT=3000
NODE_ENV=production

# ESP32 Whitelist (comma-separated IPs for production mode)
# Example: ESP32_WHITELIST=192.168.1.100,192.168.1.101
# Use 'localhost,127.0.0.1,::1' for local testing
ESP32_WHITELIST=192.168.1.100

# Security Settings
# In production: only whitelisted IPs can POST data
# In development: all IPs allowed
ENABLE_IP_WHITELIST=false